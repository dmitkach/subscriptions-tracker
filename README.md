# Subscriptions Tracker

REST API сервис для управления подписками и контроля регулярных расходов.

---

## 📌 О проекте

`Subscriptions Tracker` — это backend-сервис, который позволяет:

* создавать и получать подписки (SaaS, стриминговые сервисы и т.д.)
* хранить данные в PostgreSQL
* работать через REST API

Будет реализовано:

* контроль расходов за определенный период
* обновление информации о подписке
* удаление подписок

---

## 🏗 Архитектура

Проект построен с разделением ответственности:

```
cmd/               — точка входа (инициализация сервера)
internal/
  ├── handlers/    — HTTP слой (REST API)
  ├── migrations/  — SQL миграции
  ├── db/          — работа с БД
  └── server/      — запуск сервера
migrations-reset/  — сброс SQL миграций
```

### Принципы:

* Чистая архитектура (разделение handler → service → repository)
* Изоляция бизнес-логики от HTTP и базы данных
* UUID как primary key
* Конфигурация через `.env`

---

## 🛠 Технологический стек

* **Go**
* **Gorilla Mux** — роутинг
* **PostgreSQL**
* **Docker / Docker Compose**
* **UUID**
* **godotenv**
* SQL migrations

---

## 🚀 Запуск проекта

### Клонирование

```bash
git clone https://github.com/dmitkach/subscriptions-tracker.git
cd subscriptions-tracker
```

### Запуск через Docker

```bash
docker-compose up --build
```

Сервис будет доступен:

```
http://localhost:8080
```

---

## 🔐 Конфигурация

`.env` файл:

```
DB_DSN=postgres://your_user:your_pass@db:5432/subscriptions?sslmode=disable
DB_PASSWORD=your_user
DB_USER=your_pass
```

---

## 📡 API Пример

### Создание подписки

```http
POST /subscriptions
```

```json
{
  "service_name": "Yandex Plus",
  "price": 400,
  "user_id": "60601fee-2bf1-4721-ae6f-7636e79a0cba",
  "start_date": "07-2025",
  "end_date": "08-2026"
}
```

### Получение подписки по её UUID

```http
GET /subscriptions/{id}
```

```json
{
    "id": "f4f0c2a2-9d26-478b-bc9b-fe63d2b1ffe1",
    "service_name": "Yandex Plus",
    "price": 400,
    "user_id": "60601fee-2bf1-4721-ae6f-7636e79a0cba",
    "start_date": "07-2025",
    "end_date": "08-2026"
}
```

---

## 🗄 Модель данных

```go
type Subscription struct {
	ID          uuid.UUID
	ServiceName string
	Price       int
	UserID      uuid.UUID
	StartDate   string
	EndDate     *string
}
```

---

## 🧠 Что демонстрирует этот проект

* Работа с PostgreSQL
* Реализация REST API
* Разделение слоёв приложения
* Работа с Docker
* Конфигурация через environment variables
* Базовая миграционная стратегия
